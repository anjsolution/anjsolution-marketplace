---
name: incidents
description: TMS 장애 이력을 찾거나 세거나 분석할 때 쓴다. 트리거 — "OO터널 장애 이력", "이번 달 장애 몇 건", "본부별 장애 현황", "서버 교체한 장애 찾아줘", "장애 원인 분석해줘", "대응 이력 봐줘". 터널 소속·명칭 해석만 필요하면 catalog 스킬.
---

# incidents — 장애 이력 분석

## 찾고 센다
`search_incidents` 로 찾고 `incident_stats` 로 센다.
`group_by` 는 month·category·status·highway·division·branch·tunnel ·
location·device_category·controller_sub_type.
`keyword` 는 메모와 대응 내용을 검색하며, `총건수` 만 봐도 빈도를 알 수 있다.

코드성 필터(`category`·`location`·`device_category`·`controller_sub_type`)는 응답에 나온
라벨을 그대로 넣어도 되고 한글만·코드만·enum 도 받는다 — `하드웨어 (H)` `하드웨어` `H`
`HARDWARE` 가 모두 같다. 해석 못 하면 **어느 항목인지 밝히고 유효값을 알려주는** 오류가 온다.

`incident_stats` 를 쓸 때:
- 기간을 둘 다 생략하면 **전체 기간이 아니라 올해**다
- **그룹 합계가 전체 장애 건수보다 클 수 있다.** 한 장애의 대상이 여러 터널에 걸치고
  그 터널들이 서로 다른 지사·본부일 수 있어서, `tunnel`·`branch`·`division`·`highway` 로
  묶으면 같은 장애가 그룹마다 각각 잡힌다. 합계를 "전체 장애 수" 로 소개하지 않는다
- `그룹: null` 은 미지정이다. 버리지 말고 "미지정" 으로 보고한다

## 그 밖의 축은 받아서 센다
조치 축(방법·접근)과 대응자·소요시간은 아직 `group_by` 에 없다. 데이터를 받아 직접 집계한다.

1. 최소 범위로 거른다 (기간·본부·분류·키워드)
2. `include_history=true` 로 받는다 — `조치결과` 에 `방법`·`접근`·`유형`·`대상`·`조치코드`·`비고`·`메모` 가 들어 있다
3. 그대로 세거나, 양이 많으면 JSON 파일로 저장해 센다

먼저 필터만 걸어 `총건수`·`전체페이지` 로 범위를 가늠한 뒤 정한다.

예 — "서버 교체하러 나간 장애가 어느 본부에 많나": `include_history=true` 로 받아
`조치결과.방법` 이 `교체 (C)` 인 것을 고르고, `대상[].본부` 로 센다.

## 현장 대응은 `접근` 으로 가린다 — 추정하지 않는다
`조치결과.접근` 이 `현장 (C)` · `원격 (B)` · `유선 (A)` 로 온다. 메모 본문에서 '현장'·'교체'
같은 말을 찾아 추정하던 방식은 쓰지 않는다. 그런 추정은 우리 직원이 나간 건지 지사 인원이
조치한 건지 구분하지 못한다.

## 코드는 파싱하지 않는다
`장애코드` 앞 5자리(`HL100`)와 `조치코드` 4자리(`SRCH`)는 **아래 필드를 글자로 압축한 것**이다.
같은 정보가 필드로 오므로 자리별로 뜯을 이유가 없다.

| 코드 | 구성 필드 |
|---|---|
| 장애코드 | `분류` + `발생위치` + `장치분류` + `제어기종류` |
| 조치코드 | `조치결과.대상` + `방법` + `접근` + `유형` |

값은 `한글 (코드)` 형식이다 — `하드웨어 (H)` · `관리동-서버 (L)` · `서버 (100)` · `현장 (C)`.
괄호 안이 코드 자리에 들어간 글자다. 프론트엔드 화면 표기와 같은 값이다.

## 장애 위치는 `대상` 배열에 있다
조인할 것이 없다. 본부·지사·노선이 응답에 들어 있고, 터널은 `터널코드` 로 구분된다.

```json
"대상": [{"본부":"강원본부","지사":"강릉지사","터널코드":"065110",
          "터널명":"근덕,적노,사직,남양터널","노선":"동해선(삼척-속초)"}]
```

- **원소는 채워진 칸만 키로 갖는다.** 빈 칸은 키 자체가 없다
- DB 내부 id 는 주지 않는다. 터널은 `터널코드`, 장비는 `장치번호` 가 식별자다
- 한 장애에 대상이 여럿이면 원소도 여럿이다. **서로 다른 본부·지사일 수 있으니**
  하나로 접지 말고 원소 단위로 센다
- 터널이 아닌 대상도 온다 — 본부만, 본부+지사, 그리고 시스템(`대분류`·`중분류`)
- `대상` 이 빈 배열이면 응답만으로는 위치를 알 수 없다. 모른다고 답한다

시스템 `대분류` 는 ATMS · 통합플랫폼 · 폴스타 · 교통센터 · 미분류.
`중분류` 는 그 하위 명칭이다 (예: ATMS > 통합, 통합 웹, DB).

## 도구가 실패하면
| 증상 | 뜻 | 안내 |
|---|---|---|
| 도구가 하나도 안 보임 | 플러그인 미설치 · MCP 미등록 | 아래 설치 안내 |
| 이 스킬 도구만 안 보임 | 권한 미부여 — **권한 없는 도구는 목록에 나오지 않는다** | **장애 이력 조회 권한** 을 Ai-Ops 에 요청 |
| 401 · 인증 요구 | 로그인 안 됨 · 토큰 만료 | `/mcp` 에서 재인증 (브라우저) |
| 403 · 권한 거부 | 권한 미부여 | 위와 같이 요청 |
| `ENOTFOUND` · 연결 불가 | DNS·회선 문제 (등록 문제가 아님) | 잠시 후 재시도, 계속되면 사내망 확인 |

권한을 새로 받았으면 `/mcp` 에서 **재인증**한다. 권한은 토큰에 담겨 발급되므로 갱신만으로는
늘어나지 않고, 이미 연결된 세션에는 도구 목록 변경이 통지되지 않는다.

설치는 **마켓플레이스 등록이 먼저**다. 터미널에서 (기본 `--scope user` = 전역):

```
claude plugin marketplace add anjsolution/plugins
claude plugin install tms@anjsolution
```

세션 안에서는 `/plugin marketplace add anjsolution/plugins` · `/plugin install tms@anjsolution`.
Codex 는 `codex plugin add tms@anjsolution`.
