[pytest]
# 플러그인마다 tests/ 패키지를 두므로 test_manifests.py 같은 이름이 겹친다.
# importlib 모드는 경로 기준으로 모듈명을 만들어 이름 충돌 없이 한 번에 수집한다.
# (기본 prepend 모드에서는 `pytest plugins` 가 import file mismatch 로 죽는다)
addopts = --import-mode=importlib
testpaths = plugins
